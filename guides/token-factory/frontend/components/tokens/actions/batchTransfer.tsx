import { Box } from '@mui/material';
import { tsxProps } from '@/components/tokens/tokenActionsModal';

export const BatchTransfer = ({ tokenID, tokenName }: tsxProps) => {

	return (
		<Box>

		</Box>
	)
}